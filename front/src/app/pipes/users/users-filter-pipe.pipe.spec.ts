import { UsersFilterPipePipe } from './users-filter-pipe.pipe';

describe('UsersFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new UsersFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
