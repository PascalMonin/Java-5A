import { ReservationsFilterPipePipe } from './reservations-filter-pipe.pipe';

describe('ReservationsFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ReservationsFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
