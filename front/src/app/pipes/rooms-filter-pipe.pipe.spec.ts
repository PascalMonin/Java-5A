import { RoomsFilterPipePipe } from './rooms-filter-pipe.pipe';

describe('RoomsFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new RoomsFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
